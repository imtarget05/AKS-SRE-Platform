# Terraform (never commit state or provider binaries; lock file IS committed)
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
*.tfplan
**/plan.out
**/plan.json
**/tfplan*
**/*.auto.tfvars
crash.log

# Raw local artifacts (terraform plans, scratch) — never commit; sanitized evidence goes to docs/evidence/
**/.local/
